spring:
  data:
    datasource:
      url: jdbc:postgresql://localhost:5432/ucord_db
      username: postgres
      password: postgres
    jpa:
      generate-ddl: true
      hibernate:
        ddl-auto: update
      show-sql: true